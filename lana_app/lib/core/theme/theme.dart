import 'package:flutter/material.dart';

class LanaColors {
  LanaColors._();

  static const Color primary = Colors.teal;
  static const Color accent = Colors.orange;
}

ThemeData buildLanaTheme() {
  return ThemeData(
    primarySwatch: Colors.teal,
  );
}

import 'package:flutter/material.dart';

class LanaColors {
  LanaColors._();

  static const Color primary = Colors.teal;
  static const Color accent = Colors.orange;
}

ThemeData buildLanaTheme() {
  return ThemeData(
    primarySwatch: Colors.teal,
  );
}

